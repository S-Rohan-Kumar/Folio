import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const background     = Color(0xFF0D0D0F);  // Near-black
  static const surface        = Color(0xFF1A1A1F);  // Elevated surface
  static const surfaceVariant = Color(0xFF252530);  // Cards, sheets
  static const elevated       = Color(0xFF2E2E3D);  // Modals, dialogs

  // Brand
  static const amber          = Color(0xFFE8A838);  // Primary accent
  static const amberMuted     = Color(0xFF8F6020);  // Secondary amber
  static const purple         = Color(0xFF7C5CBF);  // Secondary accent
  static const purpleMuted    = Color(0xFF3D2E60);  // Purple tint

  // Text
  static const textPrimary    = Color(0xFFF0EFE9);  // Warm white
  static const textSecondary  = Color(0xFF9C9A8E);  // Muted
  static const textHint       = Color(0xFF5C5A52);  // Hint/disabled

  // Semantic
  static const success        = Color(0xFF4CAF82);
  static const error          = Color(0xFFE85D4A);
  static const warning        = Color(0xFFE8A838);  // same as amber
  static const info           = Color(0xFF5B9BD5);

  // Battle
  static const battleRed      = Color(0xFFE85D4A);  // Rival
  static const battleBlue     = Color(0xFF5B9BD5);  // Self
}
